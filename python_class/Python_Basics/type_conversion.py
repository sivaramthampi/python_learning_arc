a = 5
a = str(a)      # Converts to string

val = 5.5
val = int(val) # Remove the decimal and return the integer value

a = [1, 2, 3, 4, 5]
a = set(a)  # Converts the list to set 

a = [1, 2, 3, 4, 5]
a = tuple(a) # Converts the list to tuple

a = [[1, 2],[3, 4]]
a = dict(a)  # Coverts the list to dict / NB: can only convert if the list is in keyvalue pairs

a = '7'
a = int(a)   # Converts string to integer 

a = list((1, 2, 3, 4)) # Passing tuple values as an parameter to convert tuple to list 